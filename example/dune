(executable
 (name main)
 (libraries stdlib)
 (modes byte))

(include ../common.dune)

; Also compile it:

(rule
 (target main.c.exe)
 (deps main.c)
 (action
  (run %{cc} -ansi -Og -g -o main.c.exe %{deps})))

(alias
 (name default)
 (deps main.c.exe))
